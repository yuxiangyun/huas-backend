# 本地任务状态

使用默认分流状态 needs-triage、needs-info、ready-for-agent、ready-for-human、wontfix。已批准的实现任务从 ready-for-agent 开始，实施时为 in-progress，完成已约定验收后为 completed。

completed 只代表满足该任务获准的验收范围；仅允许静态检查时，必须明确运行行为尚未验证。
