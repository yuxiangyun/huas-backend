# 01-classroom

What to build: 空教室服务账号认证错误映射为3005/503，保留用户JWT错误及其他故障。
Repository: server
Blocked by: none
Status: completed

验收：代码逻辑review覆盖触发条件、成功与失败分支、并发及后续任务兼容性；不依赖测试执行。原有dirty状态不被覆盖或提交。

实现：两个查询入口经同一私有 readWithServiceAccount 读取学校数据。仅捕获学校读取抛出的 AppError/CREDENTIAL_EXPIRED，转换为 SERVICE_ACCOUNT_UNAVAILABLE（3005/503）；其他错误原对象抛出。参数规范化、审计 actor、成功结果保持原合同。

架构复盘与第二轮精简：第一版将服务账号解析与读取都置于 catch 范围。复盘后将解析移出 try，使错误转换只覆盖真正的服务账号学校读取；配置或数据库解析错误保持原归属。共用私有泛型方法消除两个入口的重复映射，不扩展 domain ports、SchoolAccess 或全局错误处理中间件。

实现者逻辑审查钩子：检查两入口参数验证仍在学校访问前；解析仍每请求一次；return await 保证异步拒绝落入本地映射；仅3003被替换，3004/504、3005/503、参数错误与未知异常原样上抛；actor仅用于成功审计，JWT认证在外部中间件先执行，不进入映射范围；私有方法无共享状态，不增加并发或重试行为。L3与application L2同步，L1没有架构或顶层模块变化，不需修改。未运行测试。

主agent最终逻辑review：通过。错误归属窄化正确，参数先验、成功审计与JWT边界不变，没有新共享状态；确认下一项02独立预算与本项不冲突。

下一项：02课表统一预算，与本项服务账号错误映射无共享修改点；本项与编排文档独立提交，不推送或部署。
