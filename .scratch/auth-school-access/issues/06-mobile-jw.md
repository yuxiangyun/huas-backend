# 06：移动课表与整学期采集使用独立请求预算

**What to build:** 移动教务单周和整学期读取统一使用 SchoolAccess，Portal→H5 派生会话共享恢复，但每个调用保留独立客户端与截止时间。

**Blocked by:** 04：使用已实现的统一 Portal 目标恢复；通用执行能力由 03 提供。

**Status:** completed

## Acceptance criteria

- [x] 保留 token-only 会话、epoch 条件创建和 generation 条件失效；共享任务只产出快照。
- [x] 保留已验证的 HTTP 401 及 HTTP 200/500 加字符串 code=401 的失效证据，不扩张普通 403/协议错误。
- [x] 二次会话失效、恢复后不可用和竞争耗尽返回非交互错误，只有 CAS 明确证据触发整体退出。
- [x] 单周与整学期继续使用已验证 current 读取路径；指定学期假空态端点不升级为正式数据源。
- [x] 保留整学期既有 45 秒总预算、日期完整性校验、日历独立 24 小时窗口和失败保留旧快照。
- [x] 删除旧 mobile-jw 重试与恢复编排；生产类型检查通过。

## 验证边界

只允许生产源码类型检查和静态审查。不新增、不读取、不修改、不运行测试，不使用临时复现脚本替代测试，不访问学校上游或生产数据。

## 实施证据

MobileJwScheduleClient 委托 mobileJw 具名操作；派生恢复只共享冻结 token/epoch/generation，原 current 路径与日历整学期采集未改。旧 executor 已删除。 生产类型检查通过；仅有静态证据，未读写或运行测试。最终两轴审查见任务 08。
