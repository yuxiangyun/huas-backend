# 08：删除旧编排并完成规范、规格两轴静态验收

**What to build:** 维护者只需在统一学校访问边界修改恢复和重试规则，业务层不再依赖旧凭证管理器；交付逐项静态证据及明确的运行验证缺口。

**Blocked by:** 05、06、07：所有业务消费者迁移完成；前序依赖已包含登录、配置与 Portal。

**Status:** completed

## Acceptance criteria

- [x] 生产业务消费者不再引用旧 CredentialManager、AcademicUpstream/AcademicHttpClient 或两个旧 executor 编排；兼容层只保留仍真实需要的单向出口。
- [x] 组合根通过窄维护入口执行清理，管理统计不把新增内部状态当作业务凭证暴露。
- [x] 重复恢复与嵌套重试循环、无消费者接口和过渡桥已删除；协议解析、业务缓存与领域策略仍有明确原所有者。
- [x] 所有相关 L3、L2、L1、API 和 ADR 状态与实现一致，单文件遵守仓库长度约束。
- [x] 生产类型检查及 diff 空白检查通过；按固定基线和本规格开展 Standards、Spec 两轴静态审查并修复可确认问题。
- [x] 不读取、修改或运行测试，不执行实测、推送和部署；交付明确说明仅有类型及静态证据。

## 验证边界

只允许生产源码类型检查和静态审查。不新增、不读取、不修改、不运行测试，不使用临时复现脚本替代测试，不访问学校上游或生产数据。

## 实施证据

旧编排与过渡入口已清除；L1/L2/L3、API 与 ADR 同步。Standards 初始2项、Spec初始1项均已修复并由各审查者复核为剩余0项。生产类型检查与差异空白检查通过，详见 [review.md](../review.md)。未读写或运行测试，未实测、推送或部署。
